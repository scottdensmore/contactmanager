# ContactManager

> This file is a thin pointer. All contributor and agent guidance lives in
> **AGENTS.md** — the single source of truth every coding agent reads. Claude Code
> loads it via the import below.
>
> Edit **AGENTS.md**, not this file. A CI check (`scripts/check-claude-md.sh`, also run
> by `make check`) fails if CLAUDE.md gains content of its own — e.g. a note appended by
> Claude Code's `#` memory shortcut, which writes here. Move any such note into AGENTS.md.

@AGENTS.md
